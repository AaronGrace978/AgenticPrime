# AgenticPrime is BYOK in local dev. Paste keys into the app UI for session-only use.
# A public deployment should move provider calls behind a backend secret manager.

# Optional examples for server-side production adaptation:
OLLAMA_API_KEY=
OPENAI_API_KEY=
ANTHROPIC_API_KEY=
GEMINI_API_KEY=
OPENROUTER_API_KEY=
